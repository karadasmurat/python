Project Jupyter’s tools are available for installation via the Python Package Index.

Install JupyterLab with pip:
$ pip install jupyterlab

Once installed, launch JupyterLab with:
$ jupyter-lab

leave the terminal open and running, otherwise the notebook server would be shutdown.

Or, to run the notebook, go to the directory and run
$ jupyter notebook


- To execute a cell, hit Shift + Enter
